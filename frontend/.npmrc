# .npmrc for better CI compatibility
package-lock=true
fund=false
audit=false

# Rollup native binary handling - force JavaScript fallback
optional=false
omit=optional

# Force npm to skip platform-specific optional dependencies
target_platform=generic
target_arch=generic
