# Frontend Environment Configuration
# Copy this file to `.env` (for local dev) or configure variables in your hosting provider.

# REST API base URL for FastAPI backend.
# For local development the Vite dev server proxies to localhost.
VITE_API_BASE_URL=http://localhost:8000

# Optional: Supabase configuration (only needed if you enable Supabase integration on the frontend).
# VITE_SUPABASE_URL=https://your-project-ref.supabase.co
# VITE_SUPABASE_ANON_KEY=your-public-anon-key
# VITE_SUPABASE_EDGE_FUNCTION_SEMANTIC_SEARCH=https://your-project-ref.supabase.co/functions/v1/semantic-search
