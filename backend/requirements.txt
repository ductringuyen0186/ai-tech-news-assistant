# FastAPI Core
fastapi==0.104.1
uvicorn[standard]==0.24.0
# Removed pydantic - uses pre-built wheel in FastAPI dependency
# pydantic==2.6.4
pydantic-settings>=2.4.0  # >=2.4.0 needed by langchain-community>=0.3.20 (numpy 2.x compat)

# Environment and Configuration
python-dotenv==1.0.0

# HTTP and API
httpx==0.25.2
requests>=2.32.5  # >=2.32.5 needed by langchain-community>=0.3.28 (numpy 2.x compat)

# CORS and Middleware
python-multipart==0.0.6

# Data Processing
# pandas==2.1.4  # Temporarily commented - causes build failures on Python 3.13
numpy>=2.0.0  # Python 3.13 requires numpy 2.x

# Web Scraping and RSS Parsing
feedparser==6.0.11
beautifulsoup4==4.12.2
newspaper3k==0.2.8
lxml-html-clean==0.1.0  # Required by newspaper3k (lxml.html.clean is now separate)

# AI/ML Libraries for Embeddings and Vector Search (100% FREE from HuggingFace)
sentence-transformers>=3.0.0  # Python 3.13 compatible - uses HuggingFace models
transformers>=4.40.0  # Latest stable version - HuggingFace Transformers
torch>=2.3.0  # Python 3.13 compatible; uses CPU mode on Render
huggingface-hub>=0.20.0  # Direct access to HuggingFace model hub (FREE)

# Vector Database - ChromaDB (100% FREE, local storage)
chromadb>=0.4.22  # Python 3.13 compatible, persistent storage

# LangChain for Multi-Agent Orchestration (FREE, open source)
langchain>=0.1.0  # Core LangChain framework
langchain-community>=0.3.20  # Community integrations (>=0.3.20 dropped numpy<2 cap)
langchain-core>=0.1.23  # Core abstractions
langchain-groq>=0.0.1  # Groq LLM integration
langchain-chroma>=0.1.0  # ChromaDB integration
# Note: All LangChain packages are FREE. Only pay for LLM API calls (Groq is cheapest)

# Deep Agents SDK (Mission 2 - per-article subagents). The deepagents
# package wraps LangGraph + LangChain into a deep-agent harness; we use
# it for orchestration of the agentic research loop. langchain-ollama
# provides the ChatOllama adapter so the agent talks to local Ollama
# (gpt-oss:20b). See docs/notes/deepagents-api-surface.md for the M1
# spike + integration contract.
deepagents==0.5.7
langchain-ollama>=1.1.0

# Task Orchestration (for future implementation)
# prefect==2.14.11

# Development and Testing
pytest==7.4.3
pytest-asyncio==0.21.1
black==23.11.0
isort==5.12.0
flake8==6.1.0
mypy==1.7.1

# Database - Direct PostgreSQL connection (no ORM)
# Using psycopg3 (modern version) - fully compatible with Python 3.13
psycopg[binary]==3.2.12

# Logging and Monitoring
structlog==23.2.0

# Scheduler (daily retention cron, Milestone 3)
APScheduler>=3.10
