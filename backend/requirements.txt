# FastAPI Core
fastapi==0.104.1
uvicorn[standard]==0.24.0
# Removed pydantic - uses pre-built wheel in FastAPI dependency
# pydantic==2.6.4
pydantic-settings==2.2.1

# Environment and Configuration
python-dotenv==1.0.0

# HTTP and API
httpx==0.25.2
requests==2.31.0

# CORS and Middleware
python-multipart==0.0.6

# Data Processing
# pandas==2.1.4  # Temporarily commented - causes build failures on Python 3.13
numpy>=2.0.0  # Python 3.13 requires numpy 2.x

# Web Scraping and RSS Parsing
feedparser==6.0.11
beautifulsoup4==4.12.2
newspaper3k==0.2.8
lxml-html-clean==0.1.0  # Required by newspaper3k (lxml.html.clean is now separate)

# AI/ML Libraries for Embeddings and Vector Search
# sentence-transformers==2.2.2
# transformers==4.35.2
# torch==2.7.0  # Updated for Python 3.13 compatibility; uses CPU mode on Render

# Vector Database
# chromadb==0.4.18

# Future LangChain integration
# langchain==0.0.350
# langchain-core==0.1.0

# Task Orchestration (for future implementation)
# prefect==2.14.11

# Development and Testing
pytest==7.4.3
pytest-asyncio==0.21.1
black==23.11.0
isort==5.12.0
flake8==6.1.0
mypy==1.7.1

# Database - Direct PostgreSQL connection (no ORM)
# Using psycopg3 (modern version) - fully compatible with Python 3.13
psycopg[binary]==3.2.12

# Logging and Monitoring
structlog==23.2.0
